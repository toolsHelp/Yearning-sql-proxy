# sql-relay 使用者上手说明

English: [USAGE_EN.md](USAGE_EN.md)

sql-relay 是一个**只读 SQL 中转代理**：它伪装成 MySQL 5.7 服务端，让
DataGrip / Navicat 等标准 MySQL 客户端直连，把只读查询转发到 **Yearning 后端**
执行，再把结果按 MySQL 协议返回。后端 MySQL 的真实凭据始终由 Yearning 持有，
代理本地只保存 Yearning 的登录口令，不落盘任何数据库密码。

---

## 一、你需要什么

1. 对应你操作系统的可执行文件（从 Releases 页面下载，或按 [README.md](README.md) 自行编译）；
2. `config.yaml` 配置模板（复制一份后按你的环境填写，见下一节）；
3. 一个 MySQL 客户端（推荐 DataGrip，Navicat / DBeaver 亦可）。

| 你的系统 | 用哪个文件 |
|---|---|
| Windows 64 位 | `sql-relay-windows-amd64.exe` |
| Linux 64 位 | `sql-relay-linux-amd64` |
| macOS 64 位（Intel） | `sql-relay-darwin-amd64` |

> 程序是静态编译，**无需安装 Go 或任何运行时依赖**。

---

## 二、配置 config.yaml

打开 `config.yaml`，把这几项改成你自己的真实值（其余一般不用动）：

```yaml
proxy_password: "连接口令"                    # 你自定义的本地连接口令

yearning:
  base_url: "http://你的-yearning后端地址"     # 注意不带末尾 /
  login_user: "你的-yearning账号"
  login_password: "你的-yearning密码"
```

其余项说明：

| 项 | 含义 |
|---|---|
| `listen` | 本地监听地址，默认 `127.0.0.1:3307`，**只监听本机**，不建议改 |
| `timeout.query` | 单条查询超时，默认 120s，慢查询可调大 |
| `timeout.connect` | 建连超时，默认 20s |
| `timeout.metadata_cache` | 表结构元数据本地缓存时长，默认 5m |

---

## 三、启动代理

### Windows（PowerShell / CMD）

```powershell
cd 到本目录
.\sql-relay-windows-amd64.exe -config config.yaml
```

### Linux / macOS

```bash
cd 到本目录
chmod +x sql-relay-linux-amd64        # 仅首次需要
./sql-relay-linux-amd64 -config config.yaml
```

启动成功后，终端会打印类似：

```
Yearning 数据源: xxx (source_id=...)
sql-relay 只读代理监听 127.0.0.1:3307，伪装版本 5.7.36，可用数据源: [...]
```

**保持这个窗口不关闭**，代理就在运行。`Ctrl+C` 退出。

> **Windows 首次运行若弹出「Windows 已保护你的电脑」或提示「未知发布者」：**
> 这是因为程序没有做商业代码签名（代码签名证书需按年付费，成本较高，本项目未购买），
> 属正常现象，不是病毒。放行办法：点击弹窗里的 **更多信息** → **仍要运行** 即可。
> 若程序是从网上下载后仍被阻止，可右键文件 → **属性** → 勾选 **解除锁定** → 确定，再运行。

---

## 四、在 DataGrip 里连接

| 项 | 值 |
|---|---|
| Host | `127.0.0.1` |
| Port | `3307`（与 config 的 `listen` 一致） |
| User | **Yearning 数据源名**（如 `xx_order`，见启动日志里列的「可用数据源」） |
| Password | config.yaml 里的 `proxy_password` |
| 驱动 | 任意 MySQL 驱动（代理伪装为 5.7） |

建议在数据源设置里关掉 "Execute statements atomically"。

连接后：

- `SHOW DATABASES` 会显示该账号有权限的**所有数据源的全部库**；
- 直接 `SELECT * FROM 库名.表名` 即可跨库查询，代理按库名自动路由到正确数据源；
- `USE 库名` 后再写不带前缀的表名也可以。

---

## 五、重要限制

1. **只读**：写语句 / DDL（ALTER/CREATE/DROP 等）/ `SELECT ... INTO` / `CALL` / 加锁读
   都会被拒绝，返回 MySQL 错误。需要改数据请走 Yearning 提交工单。
2. **结果按字符串返回**：数值列的排序/筛选在客户端按字符串比较（如需精确数值请用 `CAST` 或客户端转换）。
3. 仅监听回环 + `proxy_password` 双重防护，防止本机其他进程蹭连。

代理会在本地消化一部分请求，减少打到 Yearning 的次数：

| 请求 | 处理 |
|---|---|
| `SHOW VARIABLES` / `SELECT @@xx` / `VERSION()` / `DATABASE()` / `USER()` | 按 `server_version` 与内置变量表**伪造返回**，列名与真实 MySQL 一致 |
| `SHOW FULL TABLES` / `SHOW FULL COLUMNS` / `SHOW INDEX` / `SHOW KEYS` / `SHOW CREATE TABLE` | 转发 Yearning 后**本地缓存** `timeout.metadata_cache`（默认 5 分钟） |
| `SHOW CREATE PROCEDURE/FUNCTION` | 改写为 `information_schema.routines`/`parameters` 查询，本地拼装 SHOW CREATE 结果（不缓存） |
| `information_schema` 结构表查询 | 同上，缓存 5 分钟 |
| `SHOW DATABASES` | 本地汇总所有数据源的库 |
| `SHOW GRANTS` / 权限、字符集、引擎等管理类探测 | 本地返回空结果 |

变量表只覆盖客户端常查的变量；未收录的变量返回空行，不影响连接。
由于结构元数据有缓存，刚改过的表结构要等缓存过期（或重启代理）才会刷新。

---

## 六、抓取客户端实际请求（排查补全/内省问题）

DataGrip 的补全依赖大量 metadata 探测。想知道「哪些请求被代理本地处理、哪些被屏蔽、
哪些真的打到 Yearning」，可以打开请求审计，抓一次会话后生成支持度矩阵。

### 1. 打开审计

```yaml
audit:
  enabled: true                  # 抓完记得改回 false
  path: "sql-relay-audit.jsonl"
  max_sql: 512
  keep_literals: false           # true 会保留 SQL 字面量原文（可能含业务数据）
```

默认 `keep_literals: false`：长度大于 8 的字符串字面量与不小于 6 位的数字会被替换成 `?`，
避免业务数据落盘。日志为本地 JSONL，以 `0600` 权限追加写入，重启不清空（可用系统 logrotate 轮转）。

### 2. 抓一次会话

1. 启动代理；
2. DataGrip 里新建/重连数据源，执行一次「刷新」；
3. 展开 2~3 个库，打开几张表的结构与数据（触发字段/索引/DDL 内省）；
4. 退出连接（或停掉代理）。

### 3. 生成报告

```bash
sql-relay -report sql-relay-audit.jsonl > report.md
```

报告模式只读日志、不连 Yearning，也不需要配置文件。报告包含：

| 章节 | 用途 |
|---|---|
| 类别 × 处理方式 | 主矩阵：每类请求的「本地已支持 / 本地屏蔽 / 本地伪造 / 缓存命中 / 转发 Yearning / 转发+建工单 / 只读拦截 / 不支持 / 错误」次数 |
| 命令分布 | 确认 `COM_FIELD_LIST` 是否被客户端调用、`COM_OTHER:0x1b`（COM_SET_OPTION）是否被拒 |
| SQL 指纹 Top | 出现次数最多的 SQL 原文，直接指导下一步该本地化哪条查询 |
| 错误分布 | 按 MySQL 错误码 + 类别聚合 |
| 连接时间线 | 客户端内省的顺序，用于设计 Metadata 预取 |

### 4. 怎么读这张矩阵

- **「转发Yearning」列大且耗时长** → 该类 metadata 应该本地化（Metadata Snapshot 的优先候选）。
- **「本地屏蔽」列大** → 这类请求当前返回空结果，是 DataGrip「内省级别低」的主要来源。
- **类别是 `unknown` 且不是转发** → 分类规则漏判，需要补充（`internal/classifier/kind.go`）。
- **触发建工单次数多** → metadata 探测被卷进 Yearning 审核链，优先解耦。

---

## 七、常见问题

**Q：连接报「Access denied for user ... to database ...」或「No database selected」？**
A：确认 User 填的是启动日志里「可用数据源」列表中的名字；跨库查询务必带上库名前缀（如 `库名.表名`）。

**Q：DataGrip 打开表结构显示不完整 / 提示级别低？**
A：代理是只读的，`SHOW CREATE` 只能展示业务表 DDL，真正的 ALTER 会被拦截，属正常。
若想定位具体是哪条 metadata 请求没被满足，按上一节抓一次请求再看矩阵。

**Q：查询工单提示未批准？**
A：代理会尝试自动提交一次查询工单；若 Yearning 开启了审核，需到 Yearning 审批通过后再查。

**Q：DataGrip 的函数/存储过程节点报错？**
A：`SHOW CREATE PROCEDURE/FUNCTION` 会被代理改写为 `information_schema.routines/parameters`
查询并本地拼装成 SHOW CREATE 结果返回（Yearning 的 SQL 解析器不支持 `SHOW CREATE FUNCTION`，
直接转发会报语法错误；未限定库名的存储过程会路由到错误数据源报 1305）。拼装出的 DDL
由 routines/parameters 组合而成，与真实 SHOW CREATE 输出可能有细微格式差异。
`SHOW FUNCTION/PROCEDURE STATUS` 仍会转发给 Yearning 真实执行，若后端账号没有 `mysql.proc`
的查询权限就会报错，这是后端权限限制，需在 Yearning 侧给账号授权。

**Q：改了表结构，DataGrip 里还是旧的？**
A：结构元数据有本地缓存（默认 5 分钟）。等缓存过期，或调小 `timeout.metadata_cache`、重启代理。
